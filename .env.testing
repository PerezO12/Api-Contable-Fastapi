# Configuración para testing
# Este archivo puede ser versionado ya que es solo para tests

# Información del proyecto
PROJECT_NAME=Sistema Contable API - Testing
APP_NAME=API Contable Test

# Configuración de entorno
ENVIRONMENT=testing
DEBUG=true

# Base de datos específica para testing
DB_HOST=localhost
DB_PORT=5432
DB_NAME=accounting_system_test
DB_USER=postgres
DB_PASSWORD=test_password

# CORS para testing
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:5173","http://testrunner"]

# JWT con tokens de corta duración para testing
SECRET_KEY=test-secret-key-for-testing-only
ACCESS_TOKEN_EXPIRE_MINUTES=5
ALGORITHM=HS256

# Admin para testing
DEFAULT_ADMIN_EMAIL=admin@test.local
DEFAULT_ADMIN_PASSWORD=TestAdmin123!
DEFAULT_ADMIN_FULL_NAME=Administrador Test

# Rate limiting relajado para testing
RATE_LIMIT_PER_MINUTE=1000

# Email desactivado para testing
SMTP_HOST=
EMAILS_FROM_EMAIL=

# IA desactivada para testing
OPENAI_API_KEY=
HUGGINGFACE_API_TOKEN=
TOGETHER_API_KEY=
